name: playwright API and URL test
#Когда запускаем
on:
  push:
    branches:
      - main
# ручной запуск
  workflow_dispatch:
jobs:
  DiplomTest:
    runs-on: ubuntu-latest
    env:
      # API и UI
      API_URL: ${{ secrets.API_URL }}
      UI_URL: ${{ secrets.UI_URL }}

      # Данные пользователей для тестов UI
      SAUCE_LOGIN: ${{ secrets.SAUCE_LOGIN }}
      SAUCE_PASSWORD: ${{ secrets.SAUCE_PASSWORD }}
      SAUCE_LOGIN_NEGATIVE: ${{ secrets.SAUCE_LOGIN_NEGATIVE }}

      # Данные для Airportgap API
      AIRPORTGAP_EMAIL: ${{ secrets.AIRPORTGAP_EMAIL }}
      AIRPORTGAP_PASSWORD: ${{ secrets.AIRPORTGAP_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.1
      - name: Setup Node.js environment
        uses: actions/setup-node@v6.2.0
      - name: Установка npm пакеты из package.json
        run: npm ci
      - name: Установка браузеров 
        run: npx playwright install --with-deps
      - uses: allure-framework/setup-allurectl@v1
        with:
          allure-endpoint: ${{ secrets.ALLURE_ENDPOINT }}
          allure-token: ${{ secrets.ALLURE_TOKEN }}
          allure-project-id: ${{ secrets.ALLURE_PROJECT_ID }}
      - name: Run Playwright tests
        run: allurectl watch -- npm test
        env:
          ALLURE_RESULTS: allure-results
          ALLURE_LAUNCH_NAME: "Diplom CI #${{ github.run_number }}"
        continue-on-error: true
        if: always()
      - name: Генерация single-file Allure отчета
        run: npx allure awesome ./allure-results --single-file
        continue-on-error: true
        if: always()
      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v6.0.0
        continue-on-error: true
        if: always()
        with:
          name: allure single file
          path: |
              allure-report/index.html
              allure-results
              playwright-report
        # Отправка отчета в Telegram (файл)
      - name: Send Allure report to Telegram
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -f allure-report/index.html ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
              -F chat_id="${TELEGRAM_CHAT_ID}" \
              -F document=@allure-report/index.html \
              -F caption="Allure report (single-file)"
          fi
      # Отправка текстового уведомления в Telegram
      - name: Send test summary to Telegram
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_LINK: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: node scripts/telegram-summary.js

      
